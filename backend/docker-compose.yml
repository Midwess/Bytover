version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: bitbridge
      POSTGRES_PASSWORD: bitbridgepass
      POSTGRES_DB: bitbridge
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: