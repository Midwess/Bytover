import Home from './home'

export default function Page() {
    return (
        <main className="flex h-full w-full flex-col items-center justify-between bg-black">
            <Home/>
        </main>
    )
}

