namespace shared {
  bytes process_event(AppModule module, [ByRef] bytes msg);
  bytes handle_response(AppModule module, u32 id, [ByRef] bytes res);
  bytes view(AppModule module);
};

enum AppModule {
    "Counter",
    "System",
};
